# SNRegistry-Apps
Repo for SNRegistry's Desktop and Mobile Apps

# Running Project
```
cargo install tauri-cli
```
to install tauri so you can run the project
during testing you need a react vite webserver running on "http://localhost:5173" this is the default for vite
then you do 
```
cargo tauri dev
```